#include <stdio.h>

char q[100];

int main()
{
    int x;
    scanf("%d%.c", &x);
    int p = 0;
    long long sum =- 0;
    char c = '\0';
    while ((c = getchar()) != '\n')
        if (c - '0' < x)
            q[p++] = c;
    int y;
    scanf("%d", &y);
    
    
    
}